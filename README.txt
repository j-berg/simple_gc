# simple_gc
simple gc calculator for use with fasta files

Install:
1. Add "gc" file to bin folder
2. Make gc executable ($ chmod +x gc)
3. Move gc executable to the parent bin directory
4. Add bin folder to PATH
